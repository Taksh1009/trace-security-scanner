track = "C"
pitch = "TRACE is a passive network and HTTP security scanner built entirely on Python's standard library — DNS, TCP, HTTP/HTTPS, TLS, and security-header analysis with zero third-party runtime dependencies."
