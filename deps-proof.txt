TRACE — Dependency Proof
==========================
Generated by inspecting every `import` statement in trace.py and
modules/*.py.

Command used to extract imports:
    grep -rhE "^(import|from) " trace.py modules/*.py | sort -u

Raw output:
--------------------------------------------------
from contextlib import contextmanager
from dataclasses import dataclass
from datetime import datetime, timezone
from typing import Optional
from urllib.parse import urlparse
import http.client
import socket
import ssl
import time
--------------------------------------------------

(The `from modules.xxx import ...` lines are internal imports between
TRACE's own project files, not third-party packages, and are excluded
from the list above.)

External modules imported, and their origin:
    contextlib     -> Python standard library
    dataclasses    -> Python standard library
    datetime       -> Python standard library
    typing         -> Python standard library
    urllib.parse   -> Python standard library
    http.client    -> Python standard library
    socket         -> Python standard library
    ssl            -> Python standard library
    time           -> Python standard library

Result: every external import used by the runtime artifact (trace.py
and everything under modules/) ships with the Python 3 standard
library. No `pip install` is required, and no third-party package is
imported anywhere in the runtime code path.

The only non-stdlib package referenced anywhere in this repository is
`pytest`, used exclusively for running the test suite in tests/. It
is a development-only dependency and is never imported by trace.py or
any module under modules/ — see requirements.txt and STDLIB.md for
the explicit disclosure.

Verified on: Python 3.8+ (no version-specific stdlib features used).
